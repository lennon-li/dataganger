% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-spec.R
\name{synth_spec}
\alias{synth_spec}
\title{Create a synthesis specification}
\usage{
synth_spec(
  purpose,
  level = NULL,
  n = NULL,
  roles = NULL,
  privacy = NULL,
  name_strategy = NULL,
  seed = NULL,
  engine = NULL,
  acknowledge_risk = FALSE,
  ...
)
}
\arguments{
\item{purpose}{Character. One of \code{"demo"}, \code{"development"}, or \code{"analytics"}.
If \code{NULL}, \code{\link[=synthesize_data]{synthesize_data()}} derives the engine from the objective.}

\item{level}{Character or \code{NULL}. Synthesis level: \code{"schema"} or
\code{"marginal"}. If \code{NULL}, derived from the preset.}

\item{n}{Integer or \code{NULL}. Number of rows to synthesize. If \code{NULL},
defaults to \code{nrow(original)} at synthesis time.}

\item{roles}{A \code{dataganger_roles} object or \code{NULL}. Column role assignments.}

\item{privacy}{A \code{dataganger_privacy_check} object or \code{NULL}. When
\code{stage == "pre"}, flags harden defaults (e.g. IDs dropped, free text
removed).}

\item{name_strategy}{Character or \code{NULL}. One of \code{"preserve"}, \code{"generic"},
or \code{"dictionary_only"}. If \code{NULL}, derived from the preset.}

\item{seed}{Integer or \code{NULL}. Reproducibility seed.}

\item{engine}{Character or \code{NULL}. Optional explicit synthesis engine:
\code{"internal"}, \code{"marginal"} (alias for \code{"internal"}), or \code{"synthpop"}.
If \code{NULL}, \code{\link[=synthesize_data]{synthesize_data()}} derives the engine from the objective.}

\item{acknowledge_risk}{Logical. Required to be \code{TRUE} when
\code{purpose = "analytics"}.}

\item{...}{Additional arguments passed to the spec list. Currently supports
\code{preserve_correlations}, \code{coarsen_dates}, \code{merge_rare},
\code{free_text_strategy}, \code{geography_strategy}, \code{rare_level_min_n},
\code{preserve_missingness}.}
}
\value{
An S3 object of class \code{dataganger_spec} (a named list).
}
\description{
Builds a synthesis specification from a purpose preset with optional
user overrides. The specification records the synthesis parameters and
the required engine, but does not check engine availability - that is
done by \code{\link[=synthesize_data]{synthesize_data()}}.
}
\examples{
synth_spec(purpose = "demo")
synth_spec(purpose = "development", n = 200, seed = 42)
synth_spec(purpose = "analytics", acknowledge_risk = TRUE)
}
