% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-api.R
\name{freeze_synthesis}
\alias{freeze_synthesis}
\title{Freeze a supported synthesis policy for repeated generation}
\usage{
freeze_synthesis(
  data,
  spec,
  roles = NULL,
  allowed = generation_limits(),
  store = NULL
)
}
\arguments{
\item{data}{A data frame to use during the one-time fitting operation.}

\item{spec}{A \code{dataganger_spec} created by \code{\link[=synth_spec]{synth_spec()}} with
\code{engine = "internal"}.}

\item{roles}{A \code{dataganger_roles} object. If \code{NULL}, roles are detected from
\code{data} before fitting.}

\item{allowed}{A \code{dataganger_generation_limits} object defining permitted
seed, row-count, and dataset-count ranges.}

\item{store}{A persistent private store path or an existing private store
object. It must be supplied explicitly so the handle can be reopened in a
later R session.}
}
\value{
A \code{dataganger_frozen_generator} handle. Save this handle with
\code{\link[=saveRDS]{saveRDS()}} if it must be reopened in a later R session.
}
\description{
Fits the dependency-free internal generator while the source data are in a
human-controlled session, writes the fitted state to a private store, and
returns a source-free handle containing the public contract and opaque
references. The fitted state is not included in the returned object.
}
